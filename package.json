{
  "name": "claude-agentinc-rag",
  "version": "1.0.0",
  "description": "Agentic RAG + ReAct multi-agent orchestration demo using Claude Agent SDK",
  "type": "module",
  "scripts": {
    "dev": "tsx --env-file .env src/index.ts",
    "ask": "tsx --env-file .env src/index.ts",
    "build": "tsc",
    "start": "node --env-file .env dist/index.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.89",
    "minisearch": "^7.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0"
  }
}
